{
  "name": "brw-login-lambda",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "package": "npm run build && npm ci --production"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/aws-lambda": "^8.10.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.0.0"
  }
}