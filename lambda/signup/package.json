{
  "name": "brw-signup-lambda",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "package": "npm run build && npm ci --production"
  },
  "dependencies": {
    "@aws-sdk/client-cognito-identity-provider": "^3.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.0.0"
  }
}